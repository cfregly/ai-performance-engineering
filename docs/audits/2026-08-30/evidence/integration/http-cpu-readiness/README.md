# HTTP CPU readiness followup

The final focused run passed **16 tests with one explicit skip** in 3.95 seconds on the existing macOS/arm64 Python 3.12.2 environment. This is actual local HTTP integration, not fresh pinned Linux CI acceptance. Only `code/tests/test_http_microbench.py` changed; production routes and server code were not modified.

The original test launched `dashboard/api/server.py` without its `serve` command, probed unrelated fixed port 8123 without a timeout or child-liveness check, and returned normally when readiness failed. Its three expected routes are absent from the current API. The bounded reproduction ran the original child command and exception handling with an exclusively reserved ephemeral loopback port, disabled proxies and a 0.1-second request timeout. The child printed help and exited zero without serving; the old test still returned normally. The earlier unbounded request was interrupted, and an intermediate reproducer assertion incorrectly requiring nonzero child exit failed. Both attempts and source versions remain recorded.

The retained `test_microbench_endpoints_start_stop` identity now explicitly skips with the missing disk, loopback and HTML-route coverage stated. New, separately named tests run the actual dashboard ASGI application with real Uvicorn on an exclusively inherited socket. They verify OpenAPI identity, the real disabled-campaign HTTP 503 contract and separately documented legacy-route 404 responses. They also reject an actual failed child and a killed ready server, and verify that a timed-out child is reaped. These checks do **not** restore or certify the legacy microbenchmark/export features.

The positive integration case uses the production ASGI application, not the production `serve` CLI. Requests are bounded and ignore proxy environment settings; readiness checks child liveness before and after each response. The parent holds the exclusive socket throughout the child lifetime, and output goes to files to avoid unread-pipe deadlocks. No GPU, hardware-context route, external service, installation or paid API was used.

See `receipt.json` for commands, original attempts, exact case identities, source hashes and limits; `child-logs/` retains actual child output. `ci-dependency-review.json` records the separate read-only dependency review: the unguarded Nanochat `tokenizers` import was reported to the workflow owner, who added its pin. Five strict Prometheus wire/parser cases remain explicit optional skips without `promtool`; this was not misclassified as a collection failure.
