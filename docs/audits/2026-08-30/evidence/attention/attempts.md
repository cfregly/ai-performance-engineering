# Retained attempts

1. Focused initial test run: 39 passed, 17 CUDA skipped in 2.70 s (`tests-first.log`).
2. An append/helper invocation mistakenly used `code/...` paths while cwd was already `code/`: zsh reported `no such file or directory: code/tests/test_audit_wave1_attention_regressions.py`; Python reported `FileNotFoundError: code/labs/persistent_decode/tma_extension.py`. No proposed append/helper edits ran. The subsequent existing+focused tests did run against the then-current source: 64 passed, 17 skipped in 2.31 s (`tests-expanded-before-extra-gates.log`). Retried edits with the repository-root cwd.
3. An exploratory linter-help command used nonexistent module `core.scripts.linting.check_benchmark_entrypoints` from repository root and returned `ModuleNotFoundError: No module named 'core'`. Discovered the actual `core.scripts.linting.check_benchmarks` entrypoint; proper scoped invocation passed 11 files, 0 errors/warnings (`linter-final.log`). A read of `code/labs/flashattention4/HANDOFF.md` failed because the source is root `HANDOFF.md`; the correct source was then read.
4. Expanded tests after the additional host-ownership and CUDA gates: 65 passed, 25 skipped in 4.84 s (`tests-expanded.log`).
5. First original-reproduction fixture omitted required `window_size` on `build_dense_attention_mask`; returned `TypeError: build_dense_attention_mask() missing 1 required keyword-only argument: 'window_size'`. Fixed the fixture to pass `window_size=0` for nonwindowed modes. Second invocation exited 0, stderr empty, outputs in `original_reproductions.json`. This fixture error was not a product regression.
6. Final source test/linter/syntax/diff results are retained separately. The CUDA preflight is a real CPU invocation and must remain HOLD, not pass. No CUDA/nvcc or remote hardware command ran.
